@model CourseCRUDVM
@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Slider Table</h4>

                <a asp-controller="CourseCRUD" asp-action="Create" class="btn btn-success"><i class="mdi mdi-calendar-plus">Create</i></a>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    Image
                                </th>
                                <th>
                                    Setting
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Course course in Model.Courses)
                            {
                                <tr>
                                    <td class="py-1">
                                        <img style="width:150px !important;height:150px !important " src="~/img/course/@course.Image" alt="image" />
                                    </td>
                                    <td>
                                        <a asp-controller="CourseCRUD" asp-action="Detail" asp-route-id="@course.Id" class="btn btn-success"><i class="mdi mdi-account-card-details">Detail</i></a>
                                        <a asp-controller="CourseCRUD" asp-action="Edit" asp-route-id="@course.Id" class="btn btn-primary "><i class="mdi mdi-table-edit">Edit</i></a>
                                        <a asp-controller="CourseCRUD" asp-action="Delete" asp-route-id="@course.Id" class="btn btn-danger"><i class="mdi mdi-delete-forever">Delete</i></a>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

